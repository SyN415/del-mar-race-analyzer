
# Large files tracked with Git LFS

# Track the specific large file from Playwright in venv
venv/lib/python3.12/site-packages/playwright/driver/node filter=lfs diff=lfs merge=lfs -text

# Track the specific large file from Playwright in .venv
.venv/lib/python3.12/site-packages/playwright/driver/node filter=lfs diff=lfs merge=lfs -text
